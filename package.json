{
  "name": "naif-website",
  "private": true,
  "description": "NAIF project website (Quarto)",
  "scripts": {
    "changelog": "npm exec git-cliff -- --config cliff.toml",
    "check": "prettier --check .",
    "commit": "cz",
    "deploy": "npm run site:build && npm run publish",
    "docx": "uv run quarto render --profile docx",
    "format": "prettier --write .",
    "jampack": "npx @divriots/jampack _site",
    "lint": "uv run ruff check .",
    "lint:fix": "uv run ruff check --fix .",
    "lychee-check": "lychee --config .lycheerc.toml './*.md' './*.qmd' './posts/**/*.qmd' './events/**/*.qmd' './dashboards/**/*.qmd'",
    "lychee-check:rendered": "lychee --offline --config .lycheerc.toml './_site/**/*.html'",
    "prepare": "prek install",
    "preview": "uv run quarto preview",
    "preview:fork": "uv run quarto preview --profile fork",
    "publish": "PREK_ALLOW_NO_CONFIG=1 uv run quarto publish gh-pages --no-render",
    "publish:fork": "PREK_ALLOW_NO_CONFIG=1 uv run quarto publish gh-pages --no-render --profile fork",
    "release:prepare": "npm run site:build && npm run site:archive -- --stage",
    "render": "uv run quarto render",
    "render:fork": "uv run quarto render --profile fork",
    "screenshot:clean": "node scripts/screenshot-clean.mjs",
    "screenshot:install-browsers": "npx playwright install chromium",
    "site:archive": "uv run python scripts/release_site_archive.py",
    "site:build": "npm run render && npm run jampack",
    "sync": "uv sync",
    "test": "uv run pytest",
    "test:cov": "uv run pytest --cov",
    "typecheck": "uv run ty check",
    "validate": "npm run check && npm run lint && npm run typecheck && npm run test && npm run render && npm run lychee-check:rendered"
  },
  "devDependencies": {
    "@j178/prek": "^0.3.1",
    "commitizen": "^4.3.1",
    "cz": "^1.8.2",
    "cz-conventional-changelog": "^3.3.0",
    "git-cliff": "^2.10.1",
    "playwright": "^1.58.2",
    "prettier": "^3.6.2"
  },
  "type": "module",
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
